using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class testShit : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("fuking hell namosan ");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
